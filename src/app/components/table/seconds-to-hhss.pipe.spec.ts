import { SecondsToHHssPipe } from './seconds-to-hhss.pipe';

describe('SecondsToHHssPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondsToHHssPipe();
    expect(pipe).toBeTruthy();
  });
});
